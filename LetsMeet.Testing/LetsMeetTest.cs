namespace LetsMeet.Testing
{
    public class LetsMeetTest
    {
   /*         [Fact]
            public void AddParking_WithAnParkingObject_ShouldReturnNull()
            {
                var parkingStore = new ParkingsStore();
                var user = parkingStore.AddParking(new Parking());
                Assert.True(user != "Parking save Successfully");
            }

            [Fact]
            public void DeleteParking_WithAnParkingId_ShouldReturnNull()
            {
                var parking = new Parking();
                var parkingStore = new ParkingsStore();
                var user = parkingStore.DeleteParking(parking.Id);
                Assert.True(user != "Parking was Successfully Deleted");
            }
   */
    }
}